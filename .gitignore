node_modules/
.next/
out/
.env
.env.local
.env*
.DS_Store
*.log
.scripts/autopush.log
*.tsbuildinfo
.vercel

# data pipeline: raw downloads, intermediate, and output are all
# regenerable via `npm run all` -- the committed copy the app actually
# serves lives in /public/data and /public/tiles instead
/data-pipeline/raw/
/data-pipeline/intermediate/
/data-pipeline/output/
/data-pipeline/node_modules/
